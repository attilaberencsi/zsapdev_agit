interface;
extensible;
use draft;
use side effects;
define behavior for ZRAPI_ShopTP_B alias Shop
use etag
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}